# SoundML examples

Each numbered directory is a self-contained program built by `dune build`:

| Example | Shows |
| --- | --- |
| `01-mel-spectrogram` | synthesize a signal, `Stft`/`Mel` configs, `mel_spectrogram` |

Run one with `dune exec soundml/examples/01-mel-spectrogram/main.exe`.
